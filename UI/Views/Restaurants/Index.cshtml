@model List<Restaurant>
<h1>@ViewData["Title"]</h1>

<div class="album-description container-fluid text-black">
    <link rel="stylesheet" href="styles.css">
    <h1 class="title-3">RESTAURANTS</h1>
    <h2 class="title-3">ТУТ ЗНАХОДЯТЬСЯ ВСІ РЕСТОРАНИ</h2>
</div>

<!-- ALBUM CONTENT -->
 <div class="homepage-special-offers container">
    <link rel="stylesheet" href="styles.css">
        <h3 class="special-title-2 text-center">ТУТ ВИ МОЖЕТЕ ПОДИВИТИСЯ РЕСТОРАН ЯКИЙ БИ ВИ ХТІЛИ ВІДВІДАТИ</h3>
        </div>
        
<div class="album-content">
    <div class="container">
        <div class="row">
            @foreach(var restaurant in Model)
            {
                <div id="card-food" class="col-md-4">
                    <div class="card box-shadow">
                      <img class="card-img-top" src="data:image/jpeg;base64,@Convert.ToBase64String(restaurant.Photo)" alt="@restaurant.Title"/>
                        <div class="card-body">
                            <h5 class="card-title font-weight-bold">Ресторан @restaurant.Title </h5>
                            <p class="card-text">
                                @restaurant.ShortDescription
                            </p>
                            <a asp-controller="Restaurants" asp-action="Details" asp-route-id="@restaurant.Id" class="btn btn-outline-dark" role="button">View</a>
                        </div>
                    </div>
                </div>

            }
            
           
        </div>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/restaurants.css" />
}